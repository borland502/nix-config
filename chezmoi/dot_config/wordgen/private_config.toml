# Generated by wordgen init-config.
#
# Generate words with:
#   wordgen generate
#
# Override any value with environment variables like:
#   WORDGEN_GENERATE_COUNT=5
#   WORDGEN_GENERATE_PREFIX="pre"
#   WORDGEN_GENERATE_DATASET="embedded://all.json.zst"
#
# Dataset can be embedded://all.json.zst (default), all.json, all.json.gz, or all.json.zst.

[generate]
dataset = "embedded://all.json.zst"
count = 1
min_length = 0
max_length = 0
prefix = ""
contains = ""
sources = []
seed = 0
